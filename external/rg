#!/bin/bash

tar xzf $temp -C /tmp
mv /tmp/ripgrep-$version-x86_64-unknown-linux-musl/rg $BIN_DIR/rg
mv /tmp/ripgrep-$version-x86_64-unknown-linux-musl/complete/rg.fish ~/.config/fish/dynamo/completions/rg.fish
